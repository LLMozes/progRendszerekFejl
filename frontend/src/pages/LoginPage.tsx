export default function LoginPage() {
  return (
    <section className="page">
      <span className="pill">Session login</span>
      <h1>Welcome back</h1>
      <p className="page-subtitle">
        Login form will be added next. For now, use the backend seed users to test sessions.
      </p>
      <div className="page-card">
        <strong>Planned fields</strong>
        <span>Email, password, remember me (optional)</span>
      </div>
    </section>
  );
}
